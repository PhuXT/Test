class Caculator {
    sumPow(x, n) {
        let result = x;
        for (let i = 1; i <= n; i++) {
            result += Math.pow(x, i)
        };
        return result;
    }
}
// sua

function newFeature(params) {

}
// ngoc them